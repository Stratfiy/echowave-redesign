# Screenshots still to capture

<!-- Generated by `npm run shots -- --markdown`. Do not edit by hand. -->

32 of 36 screenshot slots in the docs have no image yet.

Each slot is declared in the page with the filename it expects. Save the
capture into `docs/images/` under exactly that name and it appears — no
page edit, no rebuild config, nothing else to change.

## How to capture

- Capture at a **1440px-wide viewport** on a **2x display** so the image is
  crisp when the site scales it down. A 720px-wide screenshot looks soft.
- Use a **real account with realistic data**. An empty state or a workspace
  called `test test` reads as a product nobody uses.
- **Crop to the thing being described.** A full browser window with the OS
  menu bar in it wastes most of the reader's attention on chrome.
- **Redact** phone numbers, API keys, customer names and balances. The names
  used in the docs are Maya, QuickMart and Acme — reuse those.
- Save as **PNG**, under about 400 KB. `pngquant` or `oxipng` if it is over.

## Outstanding

### `configurations/api-keys.mdx`

- **`developer-portal-api-keys.png`** — The Developer Portal API keys screen with two or three keys listed. Keys are issued with the dcb_ prefix now — make sure none of the rows show the retired dgr_ prefix. Redact the visible key fragments.
- **`service-keys.png`** — The service keys screen. The heading and subtitle must read Decibyl, not Dograh, and no key should be named 'Dograh Production Service Keys'. Redact key fragments.

### `configurations/inference-providers.mdx`

- **`model-configuration-byok.png`** — Model configuration showing the BYOK arrangement in the CURRENT UI. Same redesign caveat as the other two on this page.
- **`model-configuration-managed.png`** — Model configuration showing the managed-models arrangement in the CURRENT UI. The tab in the old image is literally labelled 'Dograh'. Recapture against the redesigned screen.
- **`model-configuration-speech-to-speech.png`** — Model configuration showing the speech-to-speech arrangement in the CURRENT UI. The old three-tab strip (Speech to Speech / Dograh / BYOK) no longer exists, so this needs recapturing against the redesigned screen — and the surrounding prose on this page may need rewriting to match.

### `configurations/llm.mdx`

- **`llm-add-model-manually.png`** — The same dropdown scrolled to the Add Model Manually checkbox at the bottom. Provider must not read 'dograh'.
- **`llm-model-dropdown.png`** — The LLM tab of model configuration with the Model dropdown open. Current UI — the tab strip is no longer Speech to Speech / Dograh / BYOK — and the Provider must not read 'dograh'.

### `configurations/tracing.mdx`

- **`accessing-traces.png`** — A completed run panel showing Preview / Download / Trace controls. The Gathered Context JSON is visible in this shot — make sure the trace_url in it points at the Decibyl tracing host, not langfuse.dograh.com.

### `getting-started/add-tools-and-knowledge-base.mdx`

- **`knowledge-base-upload.png`** — The Knowledge Base Files list with one document (e.g. 'QuickMart returns policy.pdf') showing retrieval mode Full Document and status Completed.
- **`tools-attach-to-node.png`** — An Agent node settings panel scrolled to the Tools section, with get_cart_details selected in the picker.
- **`tools-create-http-tool.png`** — The Create Tool / HTTP API form filled in for get_cart_details: name, a full sentence description, GET method, and the dummyjson endpoint. Decibyl branding, dark theme.
- **`tools-test-call-tool-invocation.png`** — A live test transcript showing a tool call entry for get_cart_details expanded to show arguments and the JSON response, followed by the agent's spoken answer.

### `getting-started/connect-telephony.mdx`

- **`telephony-add-configuration.png`** — The Telephony → Add Configuration dialog with provider Twilio selected, name filled in, and the credential fields present but redacted.
- **`telephony-add-number.png`** — The add-phone-number form filled in: a redacted number, country selected, an inbound workflow chosen from the dropdown, Active on, and default caller ID on.

### `getting-started/first-agent.mdx`

- **`first-agent-edit-prompt.png`** — An Agent node selected on the canvas with its settings panel open on the right, showing the Prompt field with real text in it and the Allow Interruption and Tools controls below.
- **`first-agent-editor.png`** — The workflow canvas right after generation, with a 4-5 node flow visible and the editor header (Publish / Phone Call / Test Agent / Save) in shot. Dark theme.
- **`first-agent-live-transcript.png`** — A test call in progress: transcript panel with 4-6 turns of a realistic booking conversation, and the canvas behind it with the current Agent node visibly highlighted. Redact nothing — use fictional names.
- **`first-agent-test-panel.png`** — The tester panel open on the right of the canvas, Test Audio tab selected, Run Test button visible and not yet pressed.

### `getting-started/send-call-data-with-webhooks.mdx`

- **`webhook-add-node.png`** — The add-node popover scrolled to show the Webhook entry highlighted.
- **`webhook-endpoint.png`** — The Webhook node's settings panel showing the URL field filled with a webhook.site URL, method set to POST, and the authentication section visible.
- **`webhook-received-payload.png`** — A webhook.site (or equivalent) request view showing the delivered JSON body with both initial_context-derived and gathered_context-derived fields populated. Use fictional data.

### `getting-started/trigger-calls-from-your-backend.mdx`

- **`api-trigger-add-node.png`** — The add-node popover open over the workflow canvas, with API Trigger highlighted at the top of the list. Crop to the popover plus a little canvas around it.
- **`api-trigger-urls.png`** — The API Trigger node's settings panel with both URLs visible. Blur or replace the UUID.

### `voice-agent/add-to-website.mdx`

- **`embed-add-to-website-section.png`** — The agent settings panel scrolled to the Add to Website card, with the Configure Widget button visible. Crop to that card and its neighbours, not the full page.
- **`embed-configure-widget.png`** — The Configure Widget dialog with Enable Embedding on, one allowed domain entered, Floating Widget selected, and the button text and colour fields filled with Decibyl values (not 'Ask Dograh'). Save Configurations visible.
- **`embed-copy-code.png`** — The Embed Code panel of the Configure Widget dialog showing the generated script tag with Copy Code. Redact the token and make sure the URL is the production api.decibyl.ai one, not a localhost or ngrok address.
- **`embed-open-settings.png`** — The workflow editor with the settings gear button in the top-right of the canvas visible and, ideally, hovered. Crop to the top-right corner of the canvas rather than the whole screen.

### `voice-agent/agent.mdx`

- **`agent-node-panel.png`** — An Agent node settings panel with a realistic prompt filled in, Allow Interruption on, and the Extraction and Tools sections visible below.

### `voice-agent/qa.mdx`

- **`qa-node-panel.png`** — The QA node settings panel with a realistic compliance system prompt, minimum call duration 30, sample rate below 100, and the Use Workflow's LLM toggle visible.

### `voice-agent/start-call.mdx`

- **`start-call-panel.png`** — The Start Call node settings panel, scrolled to show Greeting Type, Greeting Text, the recording disclosure toggle, and the top of the Prompt field. Current UI, dark theme, Decibyl branding.

### `voice-agent/template-variables.mdx`

- **`template-variables.png`** — The Template Variables card in agent settings with one or two variables listed and the Add New Variable key/value fields below. Crop to the card — the old image included a 'Join Slack' button from the header.

### `voice-agent/webhook.mdx`

- **`webhook-node-panel.png`** — The Webhook node settings panel with POST selected, a filled endpoint URL, an authentication credential chosen, and the JSON payload template editor showing a realistic template.
