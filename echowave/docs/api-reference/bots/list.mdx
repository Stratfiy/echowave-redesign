---
title: "bots.list"
description: "Every bot in the account."
openapi: "GET /api/v1/workflow/fetch"
---

Every bot in the account.

{/* generated: prose below this line is kept */}

Returns all agents (workflows) in your organization, including both active and archived. Each item includes summary fields — use [Get an agent](/api-reference/bots/get) to retrieve the full workflow definition.
