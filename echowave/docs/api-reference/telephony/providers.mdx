---
title: "telephony.providers"
description: "Providers and what each needs."
openapi: "GET /api/v1/organizations/telephony-providers/metadata"
---

Providers and what each needs.

{/* generated: prose below this line is kept */}

Use this endpoint to discover which telephony providers Decibyl supports and the exact credential fields each one expects. The response drives the configuration form in the dashboard, and is the recommended way to build a provider-agnostic integration.
