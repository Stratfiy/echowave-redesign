---
title: "campaigns.runs"
description: "Every call the campaign placed."
openapi: "GET /api/v1/campaign/{campaign_id}/runs"
---

Every call the campaign placed.

{/* generated: prose below this line is kept */}

Returns the individual run records for each contact in the campaign. Each record includes the same fields as [agent run details](/api-reference/calls/get), including run status, duration, transcript, and recording URL.
