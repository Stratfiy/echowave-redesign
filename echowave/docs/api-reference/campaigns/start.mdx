---
title: "campaigns.start"
description: "Start dialling."
openapi: "POST /api/v1/campaign/{campaign_id}/start"
---

Start dialling.

{/* generated: prose below this line is kept */}

Transitions the campaign from `draft` or `paused` to `running`. Decibyl begins dialing contacts up to the configured `max_concurrency`.
