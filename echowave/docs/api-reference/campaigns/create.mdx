---
title: "campaigns.create"
description: "Create a campaign from a contact list and a bot."
openapi: "POST /api/v1/campaign/create"
---

Create a campaign from a contact list and a bot.

{/* generated: prose below this line is kept */}

Before creating a campaign, [upload your contacts CSV](/api-reference/contacts/upload-url) to get a `source_url`.

Set `telephony_configuration_id` to pin the campaign to a specific [telephony configuration](/api-reference/telephony). If omitted, Decibyl falls back to the organization's [default outbound configuration](/api-reference/telephony/configs-set-default-outbound) — supplying an id that doesn't exist in your organization returns `400 telephony_configuration_not_found`.

The `time_slots` field controls when Decibyl is allowed to place calls. If omitted, calls can be placed at any time. The `timezone` field applies to all time slot windows.

```json
{
  "time_slots": [
    { "day": "monday", "start": "09:00", "end": "17:00" },
    { "day": "tuesday", "start": "09:00", "end": "17:00" }
  ]
}
```

Once created, the campaign is in `draft` status. Call [Start](/api-reference/campaigns/start) to begin dialing.
