---
title: "calls.get"
description: "One run: status, outcome, artifacts."
openapi: "GET /api/v1/workflow/{workflow_id}/runs/{run_id}"
---

One run: status, outcome, artifacts.

{/* generated: prose below this line is kept */}

Returns the full run record including run status, duration, transcript URL, recording URL, gathered context, and usage/cost info.

Use the `recording_url` and `transcript_url` directly, or use the [Download endpoint](/api-reference/calls/artifact) to generate time-limited public URLs for sharing.
